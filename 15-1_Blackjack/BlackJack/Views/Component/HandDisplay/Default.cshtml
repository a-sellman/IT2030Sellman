@Model Hand

<div class="hang mb-4">
    @for (int i = 0 < Model.Count; i++)
    {
        if (i == 0 && Model.HideHoleCard)
        {
            <img src="~/images/back.svg" />
        }
        {
            <img src="~/images//@(Model.Cards[i].Name).svg" />

        }
    }
</div>