@model TripViewModel

@{
    ViewBag.Title = "Add Trip - Page One";
    ViewBag.SubHeader = "Add Trip Destination And Dates";
}

<div asp-validation-summary="All" class="text-danger"></div>

<form asp-action="Add">
    <input type"hidden" asp-for="PageNumber" />
    <div class="form-group">
        <div>
            <label asp-for="Trip.DestinationId">Destination</label>
            <a asp-action="Index" asp-controller="Manage" class="ml-4">Manage Destinations</a>
            <select asp-for="Trip.Destination" class="form-control"
                    asp-items="@(new SelectList(Model.Destinations, "DestinationId, Name"))">

                <option value="0"></option>
            </select>
            <div>
                <div>
                    <label asp-for="Trip.AccomodationId">Acccomodations</label>
                    <a asp-action="Index" asp-controller="Manage" class="ml-4">Manage Accomodations</a>
                    <select asp-for="Trip.Accomodation" class="form-control"
                            asp-items="@(new SelectList(Model.Accomodations, "AccomodationId, Name"))">

                        <option value="0"></option>
                    </select>
                    <div>

                        <label asp-for="Trip.StartDate">Start Date</label>
                        <input asp-for="Trip.StartDate" class="form-control" />
                    </div>
                    <div>
                        <label asp-for="Trip.EndDate">End Date</label>
                        <input asp-for="Trip.EndDate" class="form-control" />
                    </div>
                </div>
                <div>
                    <button type="submit" class="btn btn-dark"> Next </button>
                    <a asp-action="Cancel" class=" btn btn-outline-dark">Cancel</a>
                </div>
</form>